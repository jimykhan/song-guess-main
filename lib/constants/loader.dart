import 'package:flutter/material.dart';

class CLoader extends StatelessWidget {
  const CLoader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
